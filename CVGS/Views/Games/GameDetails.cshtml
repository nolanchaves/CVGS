@model GameViewModel
@{
    ViewData["Title"] = @Model.Title;
}

<div class="container">
    <div class="row">
        <div class="col-md-6">
            <img src="@Model.CoverImageURL" class="img-fluid" alt="@Model.Title" style="height: 700px; width: 100%; object-fit: contain;" />
        </div>

        <div class="col-md-6">
            <h1 class="text-center">@Model.Title</h1>
            <p><strong>Description:</strong> @Model.Description</p>
            <p><strong>Platform:</strong> @Model.Platform</p>
            <p><strong>Category:</strong> @Model.Category</p>
            <p><strong>Language Support:</strong> @Model.LanguageSupport</p>
            <p><strong>Price:</strong> @Model.Price.ToString("C")</p>
            <p><strong>Rating:</strong> @Model.Rating</p>
            <p><strong>Download Size:</strong> @(Model.DownloadSize / (1024 * 1024)) MB</p>

            <div class="d-flex justify-content-start mt-3">
                <button class="btn btn-success">Add to Cart</button>
            </div>
            <div class="d-flex justify-content-start mt-2">
                <a asp-controller="Wishlist" asp-action="AddWishlist" asp-route-id="@Model.GameID" class="btn btn-secondary">Add to Wishlist</a>
            </div>
            <div class="d-flex justify-content-start mt-2">
                <a asp-controller="Games" asp-action="AllGames" class="btn btn-secondary">Back to All Games</a>
            </div>
        </div>
    </div>
</div>
<div class="container mt-5">
    <h2>Reviews</h2>
    <div class="row">
        <div class="d-flex justify-content-start mt-2">
            <a asp-controller="Review" asp-action="AddReview" asp-route-id="@Model.GameID" class="btn btn-secondary">Add Review</a>
        </div>
        <br />
        @if(Model.UserReview != null)
        {
            <div>
                <div class="card" style="height: 100%;">

                    <div class="card-body d-flex flex-column">
                        <h5 class="card-title">@Model.UserReview.DisplayName</h5>
                        <p class="card-text"><strong>Rate:</strong> @Model.UserReview.Rating</p>
                        <p class="card-text">@Model.UserReview.ReviewContent</p>
                    </div>
                </div>
            </div>
        }

        @if (Model.Reviews != null){

            @foreach (var review in Model.Reviews)
            {
                <div>
                    <div class="card" style="height: 100%;">
                   
                        <div class="card-body d-flex flex-column">
                            <h5 class="card-title">@review.DisplayName</h5>
                            <p class="card-text"><strong>Rate:</strong> @review.Rating</p>
                            <p class="card-text">@review.ReviewContent</p>
                        </div>
                    </div>
                </div>
            }
        }
    </div>
</div>

